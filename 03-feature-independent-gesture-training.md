# 功能规格：独立标签手势模型训练
> 关联模块：本地手势训练工作台 | 更新日期：2026-08-06

## 功能目标

**解决的问题**：操作者按某个模型的实际用途自行定义手势类别，不被官方预置类别绑定，并获得类别互相可比较的单一分类器模型。

**目标用户**：负责采集、训练和验收手势模型的产品/算法人员，以及接收模型的 Unity 开发。

**成功标准**：每个导出模型只包含本次数据集中由操作者创建的类别；除 Model Maker 必需的 `none` 外不预置类别；测试 Top 5 均来自该模型的同一个分类头。

---

## 页面形态决策

- 页面类型：本地桌面端高密度单页工作台。
- 核心用户任务：创建标签、采集样本、训练独立模型、实时验收、交付模型。
- 信息密度：高。
- 默认布局：采集、训练、测试三个面板页内切换，1280×720 下不产生整页纵向滚动。
- 用户确认状态：用户已确认每个模型标签集合独立，不设置官方固定类别。

---

## 用户流程

### 主流程

1. 进入页面 → 系统只保证 `none` 背景标签存在。
2. 输入当前模型需要的类别名 → 系统创建对应数据目录。
3. 逐类采集并检查样本 → 系统更新计数、图库和训练就绪状态。
4. `none` 与至少一个目标类别均达到最低样本量 → 系统允许训练。
5. 配置模型名和训练参数 → 固定版本环境启动训练。
6. 训练成功 → 导出独立 `.task` 与 `model_manifest.json`。
7. 选择模型测试 → 页面展示该模型的最终分类和真实 Top 5，官方模型仅作独立基线。

### 异常流程

| 异常情况 | 系统处理方式 | 用户提示 |
|---|---|---|
| 只有 `none` | 禁止训练 | 请至少添加一个目标手势标签 |
| 任一参与类别少于 10 张 | 禁止训练 | 列出不足的类别 |
| 删除 `none` | 拒绝删除 | `none` 是 Model Maker 必需背景标签 |
| 选择不含 `none` 或只有背景类的模型 | 禁止测试 | 该模型不是可用的独立分类器 |
| 训练环境未就绪 | 禁止训练 | 先准备固定版本训练环境 |
| 训练失败 | 保留日志 | 显示退出码与日志 |

---

## 界面状态清单

| 状态名称 | 触发条件 | 界面主要内容 | 可执行操作 | 退出条件 |
|---|---|---|---|---|
| 初始态 | 只有 `none` | 背景标签、添加入口、训练禁用 | 添加目标标签 | 创建目标标签 |
| 标签空态 | 已添加目标标签但无样本 | 类别计数为 0、缺样本提示 | 采集/删除标签 | 保存样本或删除标签 |
| 采集中 | 摄像头开启 | 实时画面、当前标签、计数 | 单张/连续采集 | 关闭摄像头 |
| 数据就绪 | 每类至少 10 张 | 训练可用 | 配置并启动训练 | 启动训练或数据变化 |
| 训练中 | 训练进程运行 | 进度、轮数、日志 | 取消 | 成功/失败/取消 |
| 训练成功 | 模型导出完成 | 新模型与清单 | 刷新并测试 | 进入测试 |
| 训练失败 | 进程异常结束 | 错误状态和完整日志 | 修改后重试 | 新任务启动 |
| 可测试模型 | 模型含 `none` 和目标类别 | 启用测试、显示标签数 | 实时测试 | 停止或换模型 |
| 非分类模型 | 官方内置模型或标签不完整 | 标记不可测试 | 选择其他模型 | 选择可用模型 |

---

## 业务规则

### 标签规则

- IF 系统启动且不存在 `none`，THEN 自动创建 `none`。
- IF 用户输入 `None`、`NONE` 等大小写形式，THEN 统一映射为 `none`。
- IF 用户输入其他合法标签，THEN 保留用户输入的大小写作为模型原始标签。
- IF 用户尝试创建与已有标签仅大小写不同的标签，THEN 返回已有标签，避免同义重复。
- IF 用户删除 `none`，THEN 拒绝操作。
- IF 用户删除其他标签，THEN 删除该标签及样本前显示不可撤销确认。

### 训练规则

- IF 数据集缺少 `none`，THEN 禁止训练。
- IF 除 `none` 外没有目标类别，THEN 禁止训练。
- IF 任一现有类别少于 10 张，THEN 禁止训练并列出类别。
- IF 开始训练，THEN 当前数据集中的全部标签共同构成该次模型的独立类别集合。
- IF 之后增删标签并再次训练，THEN 新模型使用新的类别集合，已导出的旧模型不变。
- IF 训练成功，THEN 输出 `.task` 和描述该次标签快照的模型清单。

### 推理规则

- IF 模型分类头包含小写 `none` 和至少一个其他标签，THEN 标记为可测试独立模型。
- IF 展示 Top 5，THEN 所有分数来自该模型同一分类头，不合并官方基线分数。
- IF Unity 不需要 Top 5，THEN 只交付最终原始标签与真实 score。

---

## 数据字段

| 字段名 | 类型 | 来源/去向 | 必填 | 约束 |
|---|---|---|---|---|
| 标签名 | 用户输入 | 数据目录、模型元数据、Unity 返回值 | 是 | 字母、数字、`_`、`-`，1～40 字符 |
| 背景标签 | 系统配置 | 数据目录和模型 | 是 | 固定为小写 `none` |
| 样本数 | 系统计算 | 数据目录扫描 | 是 | 训练最低 10，正式建议 100～300 |
| 模型名称 | 用户输入 | 导出目录名 | 否 | 字母、数字、`_`、`-` |
| 训练参数 | 用户输入 | Model Maker HParams | 是 | 页面规定范围 |
| 模型标签快照 | 系统计算 | `model_manifest.json` | 是 | 按实际模型元数据顺序记录 |
| 真实 Top 5 | 系统计算 | 本地测试接口 | 否 | 同一分类头最多 5 条 |

---

## 列表模型

| 字段 | 展示位置 | 优先级 | 溢出策略 | 视觉表达 |
|---|---|---|---|---|
| 标签名 | 标签列表主标题 | P0 | 单行截断 | `none` 显示“背景/必需”，其余显示“模型类别” |
| 样本数 | 标签列表右侧 | P0 | 不截断 | 数字 |
| 建议数量 | 标签副标题 | P1 | 单行截断 | 进度条 |
| 删除入口 | 标签行右侧 | P1 | 不适用 | `none` 隐藏，其余显示 × |

---

## 边界与排除

**本期不做**：

- 不自动添加官方 7 类；需要时由操作者自己创建。
- 不提供多个数据集工作区；当前页面的数据目录是下一次训练的类别集合，已导出模型独立保存。
- 不从已有模型继续增量训练；每次从当前数据重新训练分类头。
- 不强制 Unity 接入 Top 5。
- 不训练手掌检测器、关键点模型和手势嵌入器。

**待后续迭代**：

- 多数据集项目管理：当需要长期并行维护多个训练集时再增加。
- 训练时勾选标签子集：当共享数据集需要导出多个组合模型时再增加。

---

## PM Review Slice

- 本阶段关键决策：每个模型标签集合独立；仅 `none` 必需；官方类别不预置。
- 被放弃的方案：强制官方 7 类与自定义手势共同训练。
- 用户确认：已确认。
- 不需要审查的执行细节：提示文案、标签排序和空状态表现。
- 进入实现条件：已满足。
